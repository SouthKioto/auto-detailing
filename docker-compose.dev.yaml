#dev: 
services:
  auto-detailing:
    image: oven/bun:1
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    command: sh -c "bun install && bun --hot src/index.ts"
